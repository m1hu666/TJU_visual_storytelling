# 天津大学历史数据可视化项目 (Tianjin University History Visualization Project)

## 📌 项目概述
本项目是一个基于 Web 技术的交互式数据可视化平台，旨在生动展示天津大学（前身北洋大学）自 1895 年建校以来的百余年辉煌历程。项目通过多维度的可视化手段，整合了学校的时间轴演变、历史人物、校园变迁、学科调整及科研成就等核心数据，为用户提供沉浸式的校史浏览体验。

## 🏗️ 架构设计
本项目采用了现代前端工程化的设计理念：
- **ES6+ Modules (ESM)**: 全面采用 JavaScript 模块化标准，所有核心逻辑均使用 `import/export` 语法重构为独立模块，解决了传统脚本依赖混乱的问题
- **关注点分离 (SoC)**: 严格执行 `HTML` (结构)、`CSS` (表现)、`JavaScript` (逻辑) 与 `Data` (数据) 的四层分离架构
- **数据驱动**: 页面内容通过独立的 JS 数据文件（如 `history_data.js`, `science_data.js`, `campus_data.js`）动态生成，数据与视图完全解耦
- **模块化组织**: 核心功能拆分为独立模块（`map.js`, `renderer.js`, `state.js`, `ui.js`, `utils.js`），提升代码可维护性和可测试性

## 📂 详细目录结构

```text
code/
├── time.html                               # [入口] 3D 时间轴主导航页
├── README.md                               # 项目说明文档
│
├── css/                                    # [全局样式]
│   └── time.css                            # 时间轴主页核心样式（CSS3D、粒子系统）
│
├── js/                                     # [全局脚本 - ES Modules 架构]
│   ├── time.js                             # 时间轴主页核心逻辑（主入口）
│   ├── time_legacy.js                      # 旧版逻辑备份（已停用）
│   ├── data/                               # 时间轴数据层
│   │   ├── time_config.js                  # 配置常量（主题、地理坐标、渲染参数）
│   │   └── time_events.js                  # 时间轴事件数据
│   └── modules/                            # 功能模块（关注点分离）
│       ├── map.js                          # D3.js 地图渲染与交互
│       ├── renderer.js                     # Canvas 绘制逻辑（节点、粒子）
│       ├── state.js                        # 应用状态管理
│       ├── ui.js                           # UI 交互（菜单拖拽、事件绑定）
│       └── utils.js                        # 工具函数（投影计算、曲线生成）
│
├── timeline/                               # [资源] 时间轴节点图片
├── raw_picture/                            # [资源] 原始图片归档
├── processed_picture/                      # [资源] 处理后图片归档
│
├── history_people/                         # [模块] 历史人物/杰出校友
│   ├── history_wheel.html                  # 主页面
│   ├── record.txt                          # 开发记录/笔记
│   ├── css/
│   │   └── history_wheel.css               # 模块样式（3D 轮盘、力导向图）
│   ├── js/
│   │   ├── history_wheel.js                # 模块逻辑（ES Module）
│   │   └── data/
│   │       └── history_data.js             # 人物数据（姓名、生平、关系、坐标）
│   ├── images/                             # 人物头像资源（.jpg/.webp）
│   └── introduction/                       # (旧数据/介绍文档)
│       └── data.json                       # 遗留数据格式
│
├── Weijinlu_Campus/                        # [模块] 卫津路校区
│   ├── Weijinlu_Campus.html                # 主页面
│   ├── resize_images_to_building_tiannan.py # 图片处理工具脚本
│   ├── css/
│   │   └── Weijinlu_Campus.css             # 模块样式（雷达扫描、卡片布局）
│   ├── js/
│   │   ├── Weijinlu_Campus.js              # 模块逻辑（ES Module）
│   │   ├── Weijinlu_Campus_legacy.js       # 旧版逻辑备份
│   │   └── data/
│   │       └── campus_data.js              # 建筑数据（历史、图片、介绍）
│   ├── pictures/                           # 建筑展示图片
│   ├── processed_picture/                  # 模块专用处理后图片
│   └── raw_picture/                        # 模块专用原始图片
│
├── Beiyangyuan_Campus/                     # [模块] 北洋园校区
│   ├── Beiyangyuan_Campus_completed.html   # 主页面
│   ├── resize_images.py                    # 图片处理工具脚本
│   ├── css/
│   │   └── Beiyangyuan_Campus_completed.css # 模块样式
│   ├── js/
│   │   ├── Beiyangyuan_Campus_completed.js # 模块逻辑（ES Module）
│   │   ├── Beiyangyuan_Campus_legacy.js    # 旧版逻辑备份
│   │   └── data/
│   │       └── content.js                  # 校区内容数据
│   ├── processed_picture/                  # 校区图片资源
│   └── raw_picture/                        # 原始图片资源
│
├── departmental_adjustment/                # [模块] 院系调整
│   ├── departmental_adjustment.html        # 主页面
│   ├── resize_images_to_yanjing.py         # 图片处理工具脚本
│   ├── css/
│   │   └── departmental_adjustment.css     # 模块样式（ECharts 主题定制）
│   ├── js/
│   │   ├── departmental_adjustment.js      # 模块逻辑（ES Module，ECharts+D3.js 混合）
│   │   ├── departmental_adjustment_legacy.js # 旧版逻辑备份
│   │   ├── departmental_adjustment.js.bak  # 备份文件
│   │   └── data/
│   │       ├── dept_config.js              # 配置数据（学校坐标、图标映射、颜色）
│   │       └── dept_data.js                # 院系调整历史数据
│   ├── college/                            # 学院/院系相关图片资源
│   └── __pycache__/                        # Python 缓存目录
│
└── Scientific_Research_Achievements/       # [模块] 科研成就
    ├── Scientific_Research_Achievements.html # 主页面
    ├── css/
    │   └── Scientific_Research_Achievements.css # 模块样式（大屏可视化布局）
    ├── js/
    │   ├── Scientific_Research_Achievements.js # 模块逻辑（ES Module，D3.js 多图表）
    │   ├── Scientific_Research_Achievements_legacy.js # 旧版逻辑备份
    │   └── data/
    │       └── science_data.js             # 科研项目数据（项目名、学院、介绍）
    └── science.xlsx                        # 科研数据源 Excel 文件（原始数据）
```

## 🚀 功能模块详解

### 1. 3D 时间轴导航 (`time.html`)
- **核心体验**: 采用 3D 透视视角的"时间河流"设计，基于 Canvas 实时渲染，用户通过滚动或点击沿时间轴穿梭
- **技术实现**:
  - Canvas 多层渲染架构（背景层、特效层、主渲染层）
  - 3D 透视投影算法实现深度感（Z 轴坐标映射）
  - 粒子系统动态生成"星际尘埃"背景效果（120+ 粒子实时运算）
- **空间跳跃**: 类似科幻电影的 "Warp Jump" 转场效果，CSS 遮罩层 + Canvas 光速线条动画
- **地理演变**: 集成 D3.js 地理投影（Mercator 投影），动态展示学校在不同历史时期的地理位置迁移路径
- **响应式交互**: 自定义全息十字准星光标，HUD（抬头显示）科技风 UI

### 2. 历史人物谱系 (`history_people`)
- **3D 交互轮盘**: 使用 CSS3D Transforms (`rotateX`, `translateZ`) 实现 Coverflow 风格的人物选择器，沉浸感强
- **社交关系图谱**: 
  - 集成 D3.js Force-Directed Graph (力导向图)
  - 动态计算人物之间的显性与隐性关联（如师生、同僚、同期校友等）
  - 节点拖拽、缩放、碰撞检测算法
- **数据可视化**:
  - **生平轨迹**: 基于 D3.js 地理投影的飞线地图，展示人物的求学与任职足迹
  - **富文本生平**: 打字机效果 (`setTimeout` + 递归) 呈现人物传记，增强阅读仪式感
  - **图片懒加载**: `IntersectionObserver` API 优化性能，支持 `.jpg` → `.webp` 格式回退

### 3. 卫津路老校区 (`Weijinlu_Campus`)
- **科技感 UI**: 模拟雷达扫描界面，通过 Canvas 绘制动态扫描线与波纹效果
- **建筑档案**: 交互式卡片展示老校区数座标志性建筑的历史信息与现状
- **图片轮播**: 自动轮播 + 手动切换，CSS 渐变过渡效果（`opacity` + `transition`）
- **工程化重构**: 该模块是项目 ES Modules 架构的标杆，完整采用了 MVC 模式设计
- **D3.js 增强**: 历史图表、建筑分布可视化

### 4. 北洋园新校区 (`Beiyangyuan_Campus`)
- **现代建筑群展示**: 聚焦新校区的功能分区与现代建筑群
- **3D 建筑模型**: 使用 D3.js SVG 渲染建筑平面图与立体结构
- **数据可视化**: 展示校区规模、学院分布等统计数据

### 5. 院系调整沿革 (`departmental_adjustment`)
- **多图表联动**: ECharts + D3.js 混合架构
  - **桑基图**: 展示 1952 年院系大调整的学科流向（天大 ↔ 其他高校）
  - **地理迁移图**: 可视化学科跨省流转路径
  - **力导向图**: D3.js 绘制学科关系网络
  - **时间轴筛选**: ECharts Timeline 组件实现年份切换
- **数据复杂度**: 处理数百条院系调整记录，支持实时过滤与筛选
- **三库集成**: ECharts (主图表) + D3.js (关系图) + jQuery (交互逻辑)

### 6. 科研成就展示 (`Scientific_Research_Achievements`)
- **可视化方案**:
  - **矩形树图 (Treemap)**: D3.js `d3.treemap()` 展示成果按学院的权重分布
  - **旭日图 (Sunburst)**: D3.js `d3.partition()` 实现层级钻取（学院 → 具体项目）
  - **力导向图**: 展示跨学院科研合作网络
- **交互特性**:
  - 点击节点定位相关成果卡片
  - 卡片网格布局 (CSS Grid) 自适应不同屏幕
  - Hover 悬浮显示详细信息工具提示
- **数据处理**: 自动聚合、分组、颜色映射（15+ 学院色彩体系）

## 🛠️ 技术栈详解

### 前端核心技术
*   **HTML5**: 语义化标签、Canvas API、数据属性 (data-*)
*   **CSS3**: 
    - CSS Grid & Flexbox 布局系统
    - CSS3D Transforms (3D 旋转、透视、景深效果)
    - CSS 动画 & 过渡效果 (Keyframes, Transitions)
    - CSS 变量 (Custom Properties) 用于主题管理
    - Google Fonts 字体集成 (Orbitron, Rajdhani)
*   **JavaScript (ES6+)**:
    - ES Modules (import/export)
    - 箭头函数、模板字符串、解构赋值
    - Promise、async/await 异步编程
    - 类与模块化设计模式

### 数据可视化库
*   **D3.js (v7)**:
    - **力导向图 (Force-Directed Graph)**: `d3.forceSimulation()` 构建人物关系网络与科研合作图谱
    - **地理投影 (Geo Projection)**: `d3.geoMercator()` 实现中国地图可视化，标注校区位置与历史迁移路径
    - **数据转换**: `d3.hierarchy()`, `d3.partition()` 用于树形数据结构（旭日图、矩形树图）
    - **交互增强**: 缩放 (`d3.zoom()`)、拖拽 (`d3.drag()`) 行为绑定
    - **GeoJSON 数据处理**: 加载并渲染中国省级行政区划边界
*   **Apache ECharts (v5.4.3)**: 
    - 桑基图 (Sankey Diagram) 展示院系调整流向
    - 迁移地图 (Map with Lines) 可视化学科流转轨迹
    - 时间轴组件 (Timeline) 实现年份筛选交互
*   **HTML5 Canvas API**: 
    - 粒子系统 (Particle System) 实现"星际穿梭"动态背景
    - 3D 河流时间轴渲染 (透视投影计算)
    - 雷达扫描效果（卫津路校区模块）

### 辅助技术
*   **jQuery (v3.6.0)**: DOM 操作简化（院系调整模块）
*   **Python (Pillow/PIL)**: 图像批处理脚本，用于自动裁剪、压缩和格式转换图片资源
*   **JSON & JavaScript Objects**: 数据存储格式，支持热更新

### 浏览器 APIs
*   **Canvas 2D Context**: 高性能图形渲染
*   **requestAnimationFrame**: 流畅动画循环
*   **IntersectionObserver**: 性能优化的视口监听
*   **CSS Object Model (CSSOM)**: 动态样式操作

## 💻 运行指南
本项目为**纯静态 (Static Only)** 架构，无需配置后端服务器或数据库（Node.js/Python 等）。

### 快速启动
1.  **本地访问**: 直接双击根目录下的 [time.html](time.html) 文件即可在浏览器中运行
2.  **HTTP 服务器** (推荐): 为确保 ES Modules 正常加载，建议通过本地服务器访问：
    ```bash
    # Python 3
    python -m http.server 8000
    
    # Node.js (需安装 http-server)
    npx http-server -p 8000
    
    # VS Code Live Server 扩展
    # 右键 time.html -> "Open with Live Server"
    ```
    然后访问 `http://localhost:8000/time.html`

### 浏览器兼容性
*   **推荐浏览器**: Chrome 90+, Edge 90+, Firefox 88+, Safari 14+
*   **必需特性**: ES6 Modules, Canvas 2D, CSS3D Transforms, Flexbox/Grid
*   **最佳体验**: PC 端访问（大屏幕 + 鼠标交互），分辨率 ≥ 1920×1080
*   **注意事项**: 
    - 部分浏览器可能限制 `file://` 协议加载 ES Modules，此时需使用 HTTP 服务器
    - 移动端浏览体验受限（触控交互未完全适配）

### 导航说明
*   **主页导航**: 通过 `time.html` 时间节点进入各子模块
*   **返回主页**: 所有子模块左上角均设有 "返回首页" / "RETURN TO SYSTEM" 按钮
*   **交互提示**: 鼠标滚轮控制时间轴前进/后退，点击节点跳转模块

## 📝 维护与扩展指南

### 数据更新
若需更新内容，直接编辑对应模块 `js/data/` 目录下的 `.js` 数据文件，无需修改 HTML/CSS 结构：

*   **时间轴事件**: [js/data/time_events.js](js/data/time_events.js)
*   **人物信息**: [history_people/js/data/history_data.js](history_people/js/data/history_data.js)
*   **建筑数据**: [Weijinlu_Campus/js/data/campus_data.js](Weijinlu_Campus/js/data/campus_data.js)
*   **院系调整**: [departmental_adjustment/js/data/dept_data.js](departmental_adjustment/js/data/dept_data.js)
*   **科研成果**: [Scientific_Research_Achievements/js/data/science_data.js](Scientific_Research_Achievements/js/data/science_data.js)

**数据格式示例**:
```javascript
// time_events.js
export const timelineData = [
    { year: 1895, title: "北洋大学堂成立", location: "天津" },
    // ...
];

// history_data.js
export const RAW_DATA = [
    { 
        name: "张太雷", 
        tju_years: [1915, 1918],
        birth_location: [119.95, 31.77],
        life_places: [[116.40, 39.90], [119.95, 31.77]],
        intro: "中国共产党早期领导人..."
    }
];
```

### 图片处理
若需添加新图片，可使用各模块下的 Python 脚本自动调整尺寸和格式：

```bash
# 卫津路校区建筑图片处理
cd Weijinlu_Campus
python resize_images_to_building_tiannan.py

# 北洋园校区图片处理
cd Beiyangyuan_Campus
python resize_images.py

# 院系调整模块图片处理
cd departmental_adjustment
python resize_images_to_yanjing.py
```

**脚本功能**: 自动批量压缩、裁剪、格式转换（JPEG → WebP）

### 样式定制
*   **主题颜色**: 修改各模块 CSS 文件顶部的 `:root` CSS 变量
*   **布局调整**: CSS Grid/Flexbox 参数位于对应 `.css` 文件中
*   **动画速度**: 搜索 `transition`, `animation-duration` 关键词调整

### 扩展新模块
1.  复制现有模块目录结构（如 `Weijinlu_Campus`）
2.  创建独立的 HTML/CSS/JS 文件
3.  在 `js/data/` 中定义数据层
4.  在 [time_events.js](js/data/time_events.js) 中添加时间节点链接
5.  保持 ES Module 导入导出规范

### 性能优化建议
*   **图片优化**: 使用 WebP 格式，控制单张图片 < 500KB
*   **懒加载**: 大量图片使用 `IntersectionObserver` 延迟加载
*   **Canvas 优化**: 降低粒子数量（修改 `CFG.particleCount`）
*   **D3.js 数据量**: 力导向图节点建议 < 200 个
*   **ECharts 渲染**: 大数据集使用数据采样或虚拟滚动

### 常见问题
1.  **ES Modules 加载失败**: 确保通过 HTTP(S) 协议访问，而非 `file://`
2.  **图片加载 404**: 检查文件路径大小写（Linux 服务器敏感）
3.  **D3.js 地图不显示**: 确认 GeoJSON CDN 可访问性（中国大陆需备用链接）
4.  **Canvas 空白**: 检查浏览器控制台是否有 JavaScript 错误

## ✨ 设计亮点

### 视觉设计
*   **科幻极客风格**: 深色背景 + 霓虹色彩体系 (青蓝 #00f3ff, 金橙 #ffaa33, 品红 #ff0055)
*   **沉浸式体验**: 全屏 Canvas 渲染 + 无边框设计 + 自定义光标
*   **动态背景**: 实时粒子系统模拟星际穿梭效果，随滚动速度动态变化
*   **类比电影叙事**: "Warp Jump" 转场效果借鉴科幻电影的视觉语言

### 交互创新
*   **3D 空间感**: 利用透视投影算法，在 2D Canvas 上实现伪 3D 深度效果
*   **自然滚动控制**: 鼠标滚轮直接映射到时间轴 Z 轴位置，物理直觉强
*   **力导向图交互**: 人物关系网络支持实时拖拽重排，边会自然弯曲避让
*   **地图联动**: 时间轴事件自动高亮对应省份，视觉引导明确

### 技术创新
*   **纯前端技术栈**: 无需后端支持，静态部署即可，降低运维成本
*   **模块化架构**: ES6 标准模块化，避免全局污染，提升代码质量
*   **多库协同**: D3.js (自由度) + ECharts (开箱即用) 优势互补
*   **渐进增强**: 基础功能优先，高级特效降级处理

## 🎓 学习参考

### 技术文档
*   [D3.js 官方文档](https://d3js.org/) - 数据可视化权威指南
*   [ECharts 配置手册](https://echarts.apache.org/zh/option.html) - 图表配置详解
*   [MDN Canvas 教程](https://developer.mozilla.org/zh-CN/docs/Web/API/Canvas_API/Tutorial) - Canvas 绘图基础
*   [CSS-Tricks Flexbox Guide](https://css-tricks.com/snippets/css/a-guide-to-flexbox/) - Flexbox 布局完全指南

## 📄 许可证

本项目为教育目的创建，代码部分采用 MIT License 开源。

历史数据与图片资源版权归天津大学所有，仅供学习交流使用，未经授权不得商用。

---
**最后更新**: 2026-02-24
